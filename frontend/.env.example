# NewsAPI Key (get free key at https://newsapi.org)
VITE_NEWS_API_KEY=your_newsapi_key_here

# Backend API Configuration
# For local development, use http://localhost:3001/api/v1
# For production, use your deployed backend URL
# This is used for:
#   - Dynamic organization fetching from Every.org API
#   - RAG-powered "Ask Me Anything" chat feature (requires backend with Gemini API)
#   - MongoDB integration for donations, classifications, and user data
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Enable backend integration (set to 'true' to use MongoDB backend instead of localStorage)
# When enabled, also enables dynamic organization fetching from Every.org
VITE_ENABLE_BACKEND=true

# Every.org Integration
# Set to 'true' to enable real donation payments (opens Every.org in new tab)
# Set to 'false' for demo mode (simulated donations, no external redirects)
VITE_ENABLE_EVERY_ORG_PAYMENTS=true

# Every.org Donation Configuration
# Base URL for Every.org donations
# Staging (for testing): staging.every.org (test donations without real money)
# Production (for live): www.every.org (real donations)
VITE_DONATION_BASE_URL=staging.every.org

# Redirect URL after donation completion
# This is where users return after completing donation on Every.org
# Local development: http://localhost:5173/donation-success
# Production: https://yourdomain.com/donation-success
VITE_REDIRECT_URL=http://localhost:5173/donation-success